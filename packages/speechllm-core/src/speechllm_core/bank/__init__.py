"""Pre-rendered phrase bank: deterministic track numbering and manifest.

Import from the submodules directly (`speechllm_core.bank.manifest`,
`speechllm_core.bank.numbering`) — this package deliberately re-exports nothing.
"""
