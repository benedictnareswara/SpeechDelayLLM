# ── Core Framework ────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.1
pydantic>=2.9.0

# ── Audio Processing ──────────────────────────────────────────
sounddevice>=0.4.7
# numpy: 1.26.x has no Python 3.14 wheel; 2.x builds fine
numpy>=2.0.0

# ── Voice Activity Detection (Silero VAD via ONNX) ────────────
# onnxruntime 1.19.0 never existed; minimum available is 1.24.1
onnxruntime>=1.24.1

# ── Offline Speech Recognition ────────────────────────────────
# vosk has no Indonesian model anymore; faster-whisper tiny is used instead.
# The tiny model auto-downloads from HuggingFace on first run (~75MB).
faster-whisper>=1.0.0

# ── Gemini LLM ────────────────────────────────────────────────
google-generativeai>=0.8.0

# ── Text-to-Speech (lightweight) ──────────────────────────────
gTTS>=2.5.3

# ── Utilities ─────────────────────────────────────────────────
python-Levenshtein>=0.25.1

# ── Testing ───────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
